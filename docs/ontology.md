# Ontology (relationships)

Paper → authored_by → Person  
Paper → affiliated_with → Organization (first author + lab/company if known)  
Paper → about_topic → Topic  
Paper → introduces_model → Model (optional)  
Paper → introduces_dataset → Dataset (optional)